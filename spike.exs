defmodule Requester do
  use GenServer

  def fetch(url, from) do

  end
end

defmodule Parting do
  def valid_for_retrieval?(url) do

  end

  def handle_document(document) do

  end
end

Scanner.add_root(Parting, url)
